{
  "name": "react-native-ble-proxy",
  "version": "1.0.0",
  "description": "BLE proxy wrapper for React Native simulators/emulators",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc && npm run copy-proto",
    "copy-proto": "cp src/generated/ble_proxy.js lib/generated/ && cp src/generated/ble_proxy.d.ts lib/generated/",
    "watch": "tsc --watch",
    "clean": "rm -rf lib",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "react-native",
    "bluetooth",
    "ble",
    "simulator",
    "emulator"
  ],
  "peerDependencies": {
    "react-native": ">=0.70.0",
    "react-native-ble-plx": "^3.0.0"
  },
  "dependencies": {
    "protobufjs": "^7.2.5"
  },
  "devDependencies": {
    "@types/react-native": "^0.72.0",
    "typescript": "^5.0.0"
  }
}
